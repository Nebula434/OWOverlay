"""UI layer: design tokens (``theme``) and the overlay's custom widgets.

Every visual constant lives in :mod:`overlay.ui.theme`; widgets reference those
tokens by name so a re-theme only ever edits ``theme.py``.
"""
from __future__ import annotations
