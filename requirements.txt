# Overwatch teammate win-rate overlay — pinned runtime dependencies.
# Python 3.11+ required.

# Frameless, translucent, always-on-top overlay window + custom-painted widgets.
PySide6==6.11.1

# HTTP client for the OverFast API (no API key required).
httpx==0.28.1

# Global show/hide hotkey that works while the game holds focus.
# Optional at runtime: if it is missing the overlay stays continuously visible.
pynput==1.7.7
